# Godot 4+ specific ignores
.godot/
/android/

# Local-only override of project.godot (enables godot_ai without touching the shared plugin list)
/override.cfg

# Local-only Claude Code guidance (not shared with the team)
/CLAUDE.md
.DS_Store
